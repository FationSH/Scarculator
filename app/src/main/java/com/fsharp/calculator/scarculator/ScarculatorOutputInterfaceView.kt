package com.fsharp.calculator.scarculator

interface ScarculatorOutputInterfaceView {

    // interface to change/set equation view
    fun setEquation(equation: String)

    // interface to change/set result view
    fun setOutcome(outcome: String)

}